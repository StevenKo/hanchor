%table.cart_table{:align => "center", :cellpadding => "5", :cellspacing => "0", :width => "100%"}
  %thead
    %tr
      %td= t('cart.picture')
      %td= t('cart.name')
      %td= t('cart.unit_price')
      %td= t('cart.size')
      %td= t('cart.color')
      %td= t('cart.quantity')
      %td= t('cart.subtotal')
  %tbody
    - @cart_items.each do |item|
      %tr
        %td{:width => "90"}
          = link_to products_show_path(item.product.product_category.name_en,item.product) do
            =image_tag item.product.product_pics.first.pic, width: "90",border: 0
        %td
          = link_to item.product.product_infos.first.name,products_show_path(item.product.product_category.name_en, item.product)
          = link_to remove_cart_item_cart_index_path(item_id: item.id) do
            = image_tag("fancy_close.png")
        %td{:align => "right", :width => "90"}
          NT$
          = item.price
        %td{:align => "center", :width => "40"}
          = item.product_size.size if item.product_size
        %td{:align => "center", :width => "40"}
          = item.product_color.color
        %td{:align => "center", :width => "60"}
          = form_for item, url: change_cart_item_quantity_cart_path(item) do |f|
            = f.select :quantity, get_cart_item_quantity_selector(item), selected: item.quantity
        %td{:align => "right", :width => "90"} 
          NT$
          = item.price * item.quantity
    %tr{:bgcolor => "E9E4D4"}
      %td{:align => "right", :colspan => "6"}= t('cart.sum')
      %td{:align => "right", :style => "font-size:1.3em; font-weight:bold; color:#CC4D33"}
        NT$
        = sum_cart_items_price @cart_items
        %input{:name => "total", :type => "hidden", :value => "1890"}/
%p{:style => "margin:15px; padding-bottom: 25px;"}
  %span.button{:style => "float:left;"} 
    = link_to t('cart.continue'), ""
  %span.button{:style => "float:right;"} 
    = link_to t('cart.checkout'), checkout_cart_index_path