<% if @pic.new_record? %>
  alert("Failed to upload pic: <%= j @pic.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#paintings").append("<%= j render(@pic,{product: @product}) %>");
<% end %>
